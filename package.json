{
	"version": "1.0.0",
	"main": "dist/index.js",
	"scripts": {
		"start": "nodemon dist",
		"clean": "rm -Rf dist",
		"watch": "npm run clean && npm run assets && tsc --watch",
		"build": "npm run clean && npm run assets && tsc --build",
		"test": "jest"
	},
	"devDependencies": {
		"@babel/preset-typescript": "^7.23.3",
		"@jest/globals": "^29.7.0",
		"@types/jest": "^29.5.10",
		"@types/node": "^20.10.2",
		"nodemon": "^3.0.2",
		"ts-jest": "^29.1.1",
		"typescript": "^4.9.5"
	},
	"dependencies": {
		"ts-node": "^10.9.1"
	}
}
